import React from 'react'
import styled from 'styled-components'

const TeamMessage = () => {
    return (
        <TeamMessageContainer>
            
        </TeamMessageContainer>
    )
}

const TeamMessageContainer = styled.div`

`;

export default TeamMessage
